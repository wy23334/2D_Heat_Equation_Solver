name: heat2d
channels:
  - conda-forge
dependencies:
  - python=3.10
  - numpy=2.2
  - matplotlib=3.10
  - scipy=1.15
  # Phase 4: MPI runtime plus the compiler expected by the Open MPI wrapper.
  - openmpi=5.0
  - c-compiler
